export class Todo {
    constructor(id, text, isCompleted, isImportant) {
        this.id = id;
        this.text = text;
        this.isCompleted = isCompleted
        this.isImportant = isImportant
    }
}